function [txConfigs, globalLayout] = allocateFrequenciesOptimized(obj, txConfigs, ...
        observableRange, globalLayout)
    % allocateFrequenciesOptimized - Optimized frequency allocation
    % Placeholder for future optimization algorithms
    [txConfigs, globalLayout] = allocateFrequenciesReceiverCentric(obj, txConfigs, ...
        observableRange, globalLayout);
end
