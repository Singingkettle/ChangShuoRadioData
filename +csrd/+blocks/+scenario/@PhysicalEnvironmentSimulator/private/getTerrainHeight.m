function height = getTerrainHeight(obj, position2D)
    % getTerrainHeight - Get terrain height at 2D position (simple flat terrain model)
    %
    % Input Arguments:
    %   position2D - 2D position vector [x, y]
    %
    % Output Arguments:
    %   height - Terrain height at the given position

    height = 0; % Assume flat terrain for simplicity
end
